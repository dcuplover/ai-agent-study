定义一个项目是否基于 **Harness Engineering（驾驭工程）** 思想构建，其核心标准在于**开发重心是否从“优化提示词”转向了“设计 Agent 运行的环境”**。

根据顶级团队（如 OpenAI、Anthropic 和 Cursor）的实践以及行业共识，可以从以下三个维度（支柱）和对应的评判标准来定义：

### 1. 架构约束：从“建议”转向“物理阻断”

判断一个项目是否属于 Harness 工程，首要标准是看它如何对待规则。

- **评判标准**：规则是否通过**机械化手段强制执行**，而非仅仅写在 Prompt 里。
- **具体特征**：
    - **自定义 Linter**：项目是否拥有专为 Agent 设计的校验工具，其错误输出能够直接转化为 Agent 的修复指引。
    - **架构围栏**：系统是否在物理层面拦截不符合架构规范的代码（如依赖方向错误），迫使 Agent 在确定的工程边界内行事。
    - **约束优于指令**：开发者不再死磕“如何让 AI 听话”，而是构建让 AI “无法犯错”的围栏。

### 2. 环境闭环：从“生成内容”转向“闭环执行”

Harness 思想认为裸模型是“无手脚”的天才，必须为其提供操作系统的原语。

- **评判标准**：项目是否支持 **“写→跑→看→修”** 的自我验证循环。
- **具体特征**：
    - **自验证沙箱**：是否为 Agent 提供了隔离的执行环境（如 Docker、Bash）来运行并测试自己生成的代码。
    - **感知运行时数据**：Agent 是否能主动查询可观测性数据（如日志、性能指标、浏览器状态）来判断产出质量。
    - **评估器分离**：是否引入了独立的 **Evaluator（评估器）** 角色，打破 Agent 容易“盲目自嗨”的缺陷，通过对抗机制死磕质量。

### 3. 知识治理：从“脑中对齐”转向“仓库即现实”

Harness 项目强调信息的版本化与可发现性，即 **Repo-as-truth**。

- **评判标准**：所有影响 Agent 决策的知识（设计原则、踩坑记录、意图）是否**版本化地存在于代码仓库中**。
- **具体特征**：
    - **`AGENTS.md` 或 `CLAUDE.md`**：项目根目录是否有记录 Agent 专属指令和“血淋淋踩坑记录”的规则文件。
    - **渐进式披露**：系统是否避免了把所有信息堆在一起，而是根据任务阶段动态、按需地向 Agent 注入上下文。
    - **自动熵管理（垃圾回收）**：是否有专门的 Agent 定期扫描并清理过时的文档，防止陈旧信息产生的干扰。

### 4. 扩展性评估：三个 Scaling 维度

一个成熟的 Harness 项目通常在解决以下至少一个维度的扩展问题：

- **时间维度**：能否让 Agent 连续跑几小时甚至几天而方向不偏移（通过运行时纠偏机制实现）。
- **空间维度**：能否支持几百个 Agent 并行协作且不发生冲突（通过协调架构实现）。
- **交互维度**：人类是否能以最少的干预（如仅移动工单状态）引导大量 Agent 工作（通过 Symphony 等界面实现）。

### 总结：你的项目是 Harness 吗？

你可以对照以下**黄金金句**进行自测：

> **“模型决定了 Agent 的下限，而 Harness 决定了它的上限。”**

如果你的项目在 Agent 犯错时，你的第一反应是**重塑它运行的环境**（增加规则文件、写一个自动化脚本、引入新角色），而不是修改提示词或换模型，那么这个项目就是基于 **Harness Engineering** 思想构建的。